con <- dbConnect(drv="PostgreSQL", host="", port=, user="", dbname="")
dbhost <- ""
webhost <- "" # For scping png files to webserver
webuser <- "" # For scping png files to webserver
webpath <- "" # For scping png files to webserver
githome <- "" # The local path to your git repos
par(family = "Arial")